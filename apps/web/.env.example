# Supabase (client — anon key is public by design; protect data with RLS)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# NOTE: The Mistral key must NOT live here — it ships in the browser bundle.
# Reasoning/vision now route through the API server (apps/api). See lib/reasoning.ts.
